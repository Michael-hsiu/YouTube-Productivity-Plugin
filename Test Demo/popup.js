// If linked in popup.html, is written above existing HTML, also doesn't have Chrome API access
//alert(document.domain); // Creates a popup w/ current site's URL, also has odd behavior 
              // of generating popup when clicking on extension button
//document.write('Hey extension!');   // Replaces page HTMl with string param, also adds param to top 
                    // of extension HTML
